## INTRO

## Getting Started

Begin by cloning the repository.

Use npm to get dependencies:

`npm install`


### Build
 Run with:
 
`npm run build`


### Serve

To see the app, run

`npm run start`

and navigate to `localhost:3001`.
